const searchResult = {
  tripInfos : {
      "ONWARD": []
  }
};

const paxInfo = {"ADULT": "1"};

const routeInfos = [];

const airportLists = []

const form_data = {}

export {searchResult, paxInfo, routeInfos, airportLists, form_data};